# =============================================================================
# MES 前端环境变量模板
# -----------------------------------------------------------------------------
# 用法：复制为 .env（开发）或 .env.production（生产构建时自动读取）
#   Windows : copy .env.example .env
#   Linux/Mac: cp .env.example .env
#
# 注意：Vite 只会暴露以 VITE_ 开头的变量给客户端代码，
#      且环境变量在**构建时**被静态替换，修改后必须重新 build 才会生效。
# =============================================================================

# 后端 API 基地址
#   ① 开发环境（Vite 代理，推荐）：留空或填 /api
#      → vite.config.ts 已配置 /api 代理到 http://127.0.0.1:3000，无需跨域
#   ② 前后端分离部署（Nginx 反代同源）：填 /api
#      → 由 Nginx 把 /api/ 反向代理到后端 3000 端口
#   ③ 前后端不同域名/IP：填完整地址，如 http://192.168.1.100:3000/api
#      → 此时后端 .env 的 CORS_ORIGIN 必须包含本前端的来源，否则被 CORS 拦截
VITE_API_BASE_URL=/api

# 应用标题（显示在浏览器标签与登录页，可选）
VITE_APP_TITLE=MES 车间制造执行系统
